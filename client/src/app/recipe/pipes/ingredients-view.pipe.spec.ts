import { IngredientsViewPipe } from './ingredients-view.pipe';

describe('IngredientsViewPipe', () => {
  it('create an instance', () => {
    const pipe = new IngredientsViewPipe();
    expect(pipe).toBeTruthy();
  });
});
