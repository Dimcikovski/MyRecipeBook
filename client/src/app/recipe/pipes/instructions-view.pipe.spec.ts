import { InstructionsViewPipe } from './instructions-view.pipe';

describe('InstructionsViewPipe', () => {
  it('create an instance', () => {
    const pipe = new InstructionsViewPipe();
    expect(pipe).toBeTruthy();
  });
});
